#include <iostream>
#include <cmath>
using namespace std;

int main() {
    double x, y;
    cout << "Enter the value of X: ";
    cin >> x;
    cout << "Enter the value of Y: ";
    cin >> y;
    cout << "The result of the expression is " << pow(x, y) << endl;
    return 0;
}
